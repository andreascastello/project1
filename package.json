{
  "name": "project1",
  "private": true,
  "version": "1.0.0",
  "main": "index.js",
  "description": "Monorepo Turborepo pour apps web/mobile/backend",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "echo \"Error: no test specified\" && exit 0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "turbo": "^2.5.6"
  }
}
